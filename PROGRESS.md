# Состояние работы и передача в новую сессию

Обновлено 26.07.2026 (вторая сессия того же дня). Каталог:
`~/Documents/Cloude Code/3d-pipeline`. Blender 5.2.0 LTS. Репозиторий на GitHub
(`github.com/pavelevgrafov/3d-pipeline`) удалён пользователем — повторно
подтверждено в этой сессии (`git fetch` → `Repository not found`).

**Эта сессия:** прогнала функциональную проверку рендером (не только
`check.py`) и подтвердила, что оба ранее найденных бага действительно
исправлены в рабочем дереве — см. «Проверено рендером в этой сессии» ниже.
Решение по витрине принято: **вариант A** — без примера, README ссылается
на `template/`. **Git-историю в этой сессии не трогали** — пользователь
явно попросил не сносить `.git` сейчас; изменения в рабочем дереве остаются
незакоммиченными. Пуш также не выполнялся.

**Следующая сессия начинается отсюда.** Когда пользователь даст отдельную
команду продолжить: снести `.git`, сделать один новый коммит с текущим
рабочим деревом (вариант A, без витрины — уже подтверждено), и только
затем, **после отдельного явного разрешения**, создать репозиторий на
GitHub и запушить. Разрешения на снос `.git` и на пуш из прошлой сессии
считать недействительными — в этой сессии пользователь оба переспросил
и отклонил на «не сейчас»/«без пуша».

---

## Цель проекта

Универсальный пайплайн для рекламных изображений предмета в Blender:
пользователь описывает предмет словами и смотрит на картинки с вариантами,
всё остальное делает код. Пользователь не работает в GUI — он смотрит на сетку
вариантов и называет номер.

Основная рабочая модель — управление Blender через агентскую систему
(текстом и референсами), а не через GUI. Открытие живого Blender для ручного
выбора ракурса — второй, опциональный путь. Референсы работают не только
на форму, но и на материалы и свет. Полное описание для README — см. раздел
ниже, готово к использованию как есть.

Пользовательская документация: [README.md](README.md), [GUIDE.md](GUIDE.md),
[LIMITS.md](LIMITS.md).

---

## Почему пересборка: история вопроса

1. Первый коммит (`5dd9eeb`) содержал пример `examples/headphones/` — работу
   пользователя из другого проекта с эмблемой, на которой товарный знак
   Mercedes (три луча), и материалы с именами `AMG Black`/`Maybach White`.
   Это не годится для публичного репозитория.
2. В прошлой сессии пример вынесли из рабочего дерева (не удалили —
   переместили в `~/Documents/Cloude Code/headphones-example-backup/`),
   вычистили упоминания из текстов, и — что важнее — **нашли и исправили
   две молчаливые ошибки самого пайплайна**, из-за которых заготовка
   (`template/`) не работала, хотя `check.py` этого не ловил.
3. Пользователь вручную удалил старый пуш на GitHub (репозиторий целиком —
   не только историю). Локальная папка `.git/` с тем же коммитом всё ещё
   на диске, её тоже предстоит снести.
4. Теперь задача: пересобрать пайплайн с нуля как чистый, рабочий,
   без чужого контента, прогнать все проверки и запушить заново.

---

## Что нужно сделать сейчас

| | Что | Готово |
|---|---|---|
| 1 | Проверка функциональности рендером (ракурс, номера вариантов) | **да**, эта сессия |
| 2 | Снести локальную git-историю: `rm -rf .git` | нет — отложено по просьбе пользователя |
| 3 | `git init`, один осмысленный коммит с текущим рабочим деревом | нет |
| 4 | Прогнать `python3 check.py` — обязателен PASS 5/5 перед коммитом | **да**, 5/5, 22/22, эта сессия |
| 5 | Создать репозиторий на GitHub (`gh repo create` либо руками) | нет |
| 6 | Запушить (`git push -u origin main`) | нет |

Порядок важен: сначала проверка функциональности рендером (готово), потом
снос `.git` и коммит, потом — только по отдельному явному разрешению — пуш.

Никакого бессрочного разрешения на снос `.git` или на пуш сейчас нет.
В этой сессии пользователь на прямой вопрос ответил «не трогать `.git`
сейчас» и «коммит без пуша» (в контексте, где сам коммит тоже отложен) —
то есть обе операции нужно переспрашивать заново в следующей сессии, а не
считать согласованными наперёд.

---

## Состояние репозитория на момент передачи

Рабочее дерево (не в git, будет закоммичено как первый коммит новой истории):

```
lib/          библиотека — предметно-независимая, обе молчаливые ошибки исправлены
template/     заготовка нового проекта — реальные числа ракурса, не заглушка
tests/        сквозной прогон на тестовой сцене
check.py      одна команда, проверяющая репозиторий целиком (без примера в PROJECTS)
README.md, GUIDE.md, LIMITS.md, CLAUDE.md, template/README.md — вычищены от примера
.gitignore    актуализирован (examples/*/renders/ → */renders/, снят мёртвый docs/-исключение)
```

Примера в репозитории нет и не будет автоматически — см. «Открытый вопрос»
ниже. `examples/` и `docs/` как директории удалены из рабочего дерева.

`python3 check.py` на момент последней проверки (до правок этой сессии) —
**5/5 PASS, 22/22** в прогоне, ~10 секунд. **Перепроверить перед коммитом**,
если в новой сессии будут какие-то правки.

Бэкап примера с наушниками (не часть репозитория, чужая собственность):
`~/Documents/Cloude Code/headphones-example-backup/` — код + 101 МБ рендеров
+ `hero.jpg`/`detail.jpg`. Не трогать, не переносить обратно в репозиторий.

---

## Решено: чем заменить пример

**Вариант A подтверждён пользователем в этой сессии.** Витрины не будет:
репозиторий уходит с одним `template/` как единственным рабочим проектом,
README ссылается на него же. Вариант B (придуманный с нуля нейтральный
пример) не делаем — не запрашивался.

---

## Найдено на проверке выполняемости в прошлой сессии — обе молчаливые, обе исправлены

Держать в голове при финальной проверке перед коммитом: `check.py` эти два
бага не ловил, потому что проверяет только «файл создан и не пустой».
Нужен взгляд на картинку, а не только PASS.

### 1. Утверждённый ракурс не работал вообще — `lib/camera.py`

`ensure_camera()` создавала констрейнт `Track To` только при первом вызове
и не переустанавливала цель при повторных. А `shot.restore()` удаляет
`CamTarget` (он в списке `JUNK`). Итог: после `restore()` камера смотрела
вертикально вниз вместо утверждённого ракурса — во всех стадиях `looks`,
`light`, `preview`, `final`. `check.py` это не ловил: вид сверху на плоский
прямоугольник тоже «файл создан и не пустой».

**Исправлено:** цель переустанавливается при каждом вызове `ensure_camera()`
([lib/camera.py:37](lib/camera.py:37)). Проверено рендером до/после.

### 2. Номера вариантов уезжали за кадр — `lib/variants.py`

`label_on()` ставила номер по фиксированным координатам в метрах
(`margin=(0.60, 0.56)`), хотя вся остальная библиотека живёт в долях охвата
кадра. При 85-мм объективе заготовки кадр на рабочей дистанции оказывался
вдвое уже этих чисел — номер рендерился за кадром, лист вариантов выходил
без номеров. А весь способ работы пользователя — «смотрю на варианты
и называю номер».

**Исправлено:** `label_on()` берёт кадр из `cam.data.view_frame()`
([lib/variants.py:22](lib/variants.py:22)) — то же, чем пользуется сам Blender;
`margin`/`size` теперь в долях охвата кадра. Проверено рендером: номера 1–4
видны в кадре.

**Повторная проверка в этой сессии (26.07.2026):** `blender -b --python
template/make.py -- /tmp/tpipe preview` и `... looks` — камера смотрит на
предмет под углом (не сверху вниз), номера 1–4 видны в левом верхнем углу
каждого тайла `looks.png`. Оба бага подтверждены исправленными визуально,
не только по `check.py`.

---

## Не трогать пока: набор света `editorial_light`

В `lib/lighting.py` лежит незакоммиченный второй набор света — светлый
разворот для матового/светлого предмета, противоположность `editorial_dark`.
Нигде не вызывается, на работоспособность пайплайна не влияет, можно
коммитить как есть (мёртвый код) или вырезать — на усмотрение новой сессии,
явного запрета уже нет, но и явного запроса доделать тоже не было.

Известное на нём: первая редакция давала пересвеченную плоскую картинку
(AgX сжимал всё в узкую светлую полосу из-за почти сплошной эмиссии фона
при `falloff=1.4`). После снижения уровней источников втрое и переноса
хотспота фона за предмет (`hotspot=(0.0, 0.05)`, `falloff=1.00`,
`backdrop_base=0.38`) сетка вариантов дала нормальный светлый разворот —
**но подтверждающий рендер после последней правки констант не запускался**,
считать набор проверенным нельзя. Отдельно: ни `editorial_dark`, ни
`editorial_light` не дают контактной тени — в `backdrop()` только задник,
пола нет.

---

## Числа заготовки для протокола

Контрольная сумма вместо `ortho_scale` (заготовка снимается перспективой,
у неё `ortho_scale = None`):

| | |
|---|---|
| Габариты объекта заготовки | 3.5461 × 1.6430 × 3.8418, диагональ 5.4803 |
| Кадр `preview` после `frame.fit` | `extent_x` (−1.773, 1.773), `extent_y` (−0.897, 0.729) |
| | `depth_z` (−15.339, −11.498), `shift` (0.0, 0.0252), `lens` 85 |
| Полигонов в заготовке | 1640 |

```
CAM_QUAT = (0.6885, 0.5777, 0.2818, 0.3358)
CAM_EYE = (4.66, -3.64, 1.04)
VIEW_DISTANCE = 6.00
AZIMUTH, ELEVATION, DISTANCE, LENS = 52.0, 10.0, 6.0, 85.0
```

Сняты с самой заготовки, не заглушка от чужого проекта — переключение
`USE_VIEWPORT` в `template/shot.py` даёт тот же кадр обоими путями.

| | |
|---|---|
| Грейд по готовому EXR | ~1 с |
| `check.py` целиком | ~10 с |
| Сетка вариантов, 4 кадра 360×480 при 64 сэмплах | ~9 с |

---

## Найдено рендером, не рассуждением (исторический раздел, не удалять)

Десять ошибок за всю историю проекта, четыре из них — молчаливые: код
не падал, а выдавал правдоподобный неверный результат. Самый частый
и самый дорогой класс ошибок в этом проекте.

1. **`snap_to_viewport()` в фоне отдавал стартовый ракурс.** При `blender -b`
   область VIEW_3D в данных всё-таки есть. Теперь проверяется
   `bpy.app.background` и бросается ошибка.
2. **`frame.extent()` считал источники света частью предмета.** Габариты
   раздувались втрое. Отсекаются по `visible_camera`.
3. **`frame.fit(by="height")` срезал широкий предмет** на вертикальном кадре,
   `by="width"` врал на любом неквадратном кадре. Пересчёт через пропорции
   кадра, по умолчанию `by="max"`.
4. **`blender -b` без файла грузит стартовую сцену** — куб и лампа попадали
   в кадр. Отсюда `scene.reset()` первой строкой каждого `make.py`.
5. `BLENDER_EEVEE_NEXT` не существует в Blender 5 — движок снова
   `BLENDER_EEVEE`.
6. `check.py` брал первую строку с префиксом `SMOKE ` вместо вердикта.
7. Ожидания теста по пассам были неверны: префикс `ViewLayer.`,
   AO — `Ambient Occlusion`, не `AO`.
8. `py_compile` с `cfile=os.devnull` падает на Python 3.14.
9. **`ensure_camera()` не переустанавливала цель `Track To`** — см. выше.
10. **`label_on()` жила в метрах, а не в долях кадра** — см. выше.

---

## Принципы, на которых всё держится

* **Смотреть, а не рассуждать.** Превью — 5 секунд, сетка вариантов — 9.
* **Утверждённые числа — в код,** а не в `.blend`: файл мог быть сохранён
  до утверждения.
* **Всё в долях охвата кадра.** Абсолютные размеры в `lib/` — ошибка;
  ошибки №2 и №10 выше ровно про это.
* **В `lib/` не попадает ничего предметного.** Проверяется тем, что тестовая
  сцена в `smoke.py` намеренно не похожа на заготовку.
* **Всё, что уходит наружу, проверяется на чужие знаки и чужую работу
  ДО отправки.** Публичный пуш плохо откатывается — этот урок стоил
  пересборки всего репозитория.
